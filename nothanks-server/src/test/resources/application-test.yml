datasources:
  default:
    url: jdbc:h2:mem:nothanks;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    driver-class-name: org.h2.Driver
    dialect: h2
    username: sa
    password: sa